"""Scout's toolset: the builtins, plus anything only Scout should be able to do.

Add Scout-specific tools here with @tool — they are picked up automatically.
"""
from pal.tools.builtin import BuiltinTools


class ScoutTools(BuiltinTools):
    pass
